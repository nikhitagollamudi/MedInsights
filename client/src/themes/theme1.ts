import { createTheme } from '@mui/material/styles';

const theme1 = createTheme({
  palette: {
    primary: {
      main: '#4caf50', // Green
    },
    secondary: {
      main: '#ffeb3b', // Yellow
    },
  },
});

export default theme1;
